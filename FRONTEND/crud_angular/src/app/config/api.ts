/**
 * URL base de la API. Debe coincidir con la URL que te muestra Visual Studio (puerto 5000).
 * En desarrollo usaremos HTTP para evitar problemas de certificados.
 */
export const API_BASE_URL = 'http://localhost:5000/api';

